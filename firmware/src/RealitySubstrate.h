#pragma once

#include <stdint.h>

/** Class-1 Omega substrate — Akashic storage, chrono-kinetic, mnemonic hive, biosymmetric, spatial, causal, eigen, genesis. */
void realitySubstrateInit();
void realitySubstrateTick(uint32_t orchestrationTick, uint32_t processedFrames);
